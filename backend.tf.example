# Optional remote-state backend. Copy this file to backend.tf only after the
# bucket in backend.tfvars already exists, then initialize with:
# terraform init -backend-config=backend.tfvars
terraform {
  backend "gcs" {}
}
